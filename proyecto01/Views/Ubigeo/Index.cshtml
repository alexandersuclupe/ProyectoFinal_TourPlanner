@model IEnumerable<proyecto01.Models.Ubigeo>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <div>
        <div>
            <h2>Lista de Ubigeos</h2>
        </div>
        <div class="col-6">
            <a asp-controller="Ubigeo" asp-action="Crear" class="btn btn-success">Nuevo Ubigeo</a>
        </div>
    </div>
    <br/>
    @if(Model.Count()>0)
    {
        <table class="table table-bordered table-striped" style="width=100%">
            <thead>
                <tr>
                    <th class="text-center">Ubigeo</th>
                    <th class="text-center">Departamento</th>
                    <th class="text-center">Provincia</th>
                    <th class="text-center">Distrito</th>
                    <th class="text-center">Editar/Eliminar</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <th>@item.ubigeo</th>
                        <th>@item.departamento</th>
                        <th>@item.provincia</th>
                        <th>@item.distrito</th>
                        <th class="text-center">
                           <div class="w-75 btn-group" role="group">
                               <a asp-controller="Ubigeo" asp-action="Editar" asp-route-Id="@item.Id" class="btn btn-primary mx-2">
                                   Editar
                               </a>
                                <a asp-controller="Ubigeo" asp-action="Eliminar" asp-route-Id="@item.Id" class="btn btn-danger mx-2">
                                    Eliminar
                                </a>
                           </div> 
                        </th>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <pNo>No existe ningun registro</pNo>
    }
</div>

