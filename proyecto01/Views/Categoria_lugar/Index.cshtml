@model IEnumerable<proyecto01.Models.Categoria_lugar>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <div>
        <div>
            <h2>Lista de Categoria Lugar</h2>
        </div>
        <div class="col-6">
            <a asp-controller="Categoria_lugar" asp-action="Crear" class="btn btn-success">Nueva Categoria Lugar</a>
        </div>
    </div>
    <br/>
    @if(Model.Count()>0)
    {
        <table class="table table-bordered table-striped" style="width=100%">
            <thead>
                <tr>
                    <th class="text-center">Nombre</th>
                    <th class="text-center">Editar/Eliminar</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <th>@item.nombre</th>
                        <th class="text-center">
                           <div class="w-75 btn-group" role="group">
                               <a asp-controller="Categoria_lugar" asp-action="Editar" asp-route-Id="@item.Id" class="btn btn-primary mx-2">
                                   Editar
                               </a>
                                <a asp-controller="Categoria_lugar" asp-action="Eliminar" asp-route-Id="@item.Id" class="btn btn-danger mx-2">
                                    Eliminar
                                </a>
                           </div> 
                        </th>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <pNo>No existe ningun registro</pNo>
    }
</div>

